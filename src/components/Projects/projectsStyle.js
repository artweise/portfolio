import styled from 'styled-components';
import { SPACING, FlexContainer } from '../../utilities/globalStyles';

export const StyledProjectsSection = styled(FlexContainer)`
  height: 95vh;
  justify-content: center-between;
  align-items: center;
  background-color: rgb(135, 228, 198);
  font-size: 30px;
`;
