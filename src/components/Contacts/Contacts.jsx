import { StyledContactsSection } from './contactsStyle';

const Contacts = () => {
  return (
    <StyledContactsSection>
      <h1>Contacts</h1>
    </StyledContactsSection>
  );
};

export default Contacts;
